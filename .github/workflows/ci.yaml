name: GitHub Actions Demo

on: [push]

jobs:
  my-first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Hello
        run: |
          echo "Hello World"
      - name: This will fail
        run: |
          echo exit 1
      
      
